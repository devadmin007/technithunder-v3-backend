'use strict';

/**
 * case-study-hero-component router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::case-study-hero-component.case-study-hero-component');
