'use strict';

/**
 * case-study-hero-component service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::case-study-hero-component.case-study-hero-component');
